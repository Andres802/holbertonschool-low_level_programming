#include <stdio.h>

int main(void)
{
	int i = 0;

	for (i = 0; i <= 9; i++)
		printf("%i", i);
	printf("\n");
	return (0);
}
#include <stdio.h>
/**
 * main - last digit
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 0;

for (i = 0; i <= 9; i++)
printf("%i\n", i);
return (0);
}
